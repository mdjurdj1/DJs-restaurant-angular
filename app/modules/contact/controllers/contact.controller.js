angular.module('angularRestaurant.contact').controller('ContactController',
  function () {

    var vm = this;

    vm.alert = () => {
      alert("Heh, we don't have an instagram yet but we're totally working on it!")
    }

  });
